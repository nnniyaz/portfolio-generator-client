import React from 'react';
import classes from './index.module.scss';

const Main = () => {
    return (
        <div className={classes.main}>
            
        </div>
    );
};

export default Main;
